// /api/stripe-webhook.js
// Placeholder - implementiraćemo u Koraku 4 (posle frontend integracije)

export default async function handler(req, res) {
  return res.status(200).json({ received: true });
}
